{
  "name": "@proagentstore/host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && wrangler dev",
    "deploy": "node build.js && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
