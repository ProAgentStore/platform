// @proagentstore/sdk/ui — shared "lovable shell" components for agent UIs.
//
// Stub. The console's reusable surface chrome (the vibe/black-box components) is
// extracted here as the agent-OS SDK forms (P1/P2), so every agent's UI feels
// coherent — mirrors @freeappstore/sdk's `./ui` subpath. React lands as a peer
// dependency when the first component moves in. See ../../PLAN-agent-os.md.

export {};
