{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"ProAgentStore Docs","text":"<p>ProAgentStore is an MCP-first marketplace and runtime platform for server-powered AI agents.</p> <p>Creators publish agent templates. Users subscribe to private agent instances. Each instance keeps user-specific state, knowledge, memory, credentials, and runtime tasks. The console, API, MCP server, connectors, and local runner all operate on that same private instance model.</p>","path":["ProAgentStore Docs"],"tags":[]},{"location":"#start-here","level":2,"title":"Start Here","text":"<ul> <li>Connectors: connect Google Drive, Google Docs, and Zoho WorkDrive at the account level, then grant selected folders or shared drives to agents.</li> <li>Triggers: configure webhooks and cron schedules for private agent instances.</li> <li>MCP: connect Codex, Claude Code, Cursor, and other MCP clients to the official ProAgentStore MCP server.</li> <li>Browser Runtime: run local browser and CLI capabilities with <code>pags up</code>.</li> <li>Architecture: understand the control plane, state plane, runtime plane, and main Cloudflare services.</li> <li>Skill Publishing: publish ProAgentStore skills for Codex and Claude users.</li> </ul>","path":["ProAgentStore Docs"],"tags":[]},{"location":"#core-model","level":2,"title":"Core Model","text":"<p>ProAgentStore separates account connections from agent permissions.</p> <ol> <li>The user connects a provider to their ProAgentStore account.</li> <li>The user grants selected folders, shared drives, repositories, mailboxes, or runtime capabilities to one or more agents.</li> <li>The user configures instance-level triggers such as webhooks or schedules when external systems should start work.</li> <li>Agents use only the granted resources through scoped platform tools.</li> <li>MCP and console actions share the same authorization and audit model.</li> </ol> <p>This is the intended model for Google Docs and Zoho WorkDrive. Connect the account once, then allow specific agents to use specific folders or shared drives.</p>","path":["ProAgentStore Docs"],"tags":[]},{"location":"#public-endpoints","level":2,"title":"Public Endpoints","text":"<pre><code>https://proagentstore.online/docs/\nhttps://proagentstore.online/llms.txt\nhttps://proagentstore.online/llms-full.txt\nhttps://proagentstore.online/openapi.yaml\nhttps://proagentstore.online/.well-known/mcp-server.json\nhttps://mcp.proagentstore.online/mcp\n</code></pre>","path":["ProAgentStore Docs"],"tags":[]},{"location":"#operating-rule-for-agents","level":2,"title":"Operating Rule For Agents","text":"<p>Agents should use ProAgentStore through MCP or documented platform APIs. They should not scrape the console or call private endpoints directly.</p>","path":["ProAgentStore Docs"],"tags":[]},{"location":"architecture/","level":1,"title":"Architecture","text":"<p>ProAgentStore is a Cloudflare-native marketplace and runtime platform for server-powered AI agents.</p>","path":["Architecture"],"tags":[]},{"location":"architecture/#repository-map","level":2,"title":"Repository Map","text":"<pre><code>platform/\n  workers/\n    api/                 Hono API Worker, AgentDO, RelayDO, Workflows\n    mcp/                 MCP server and OAuth provider\n    host/                Static host for store, console, widget, docs\n  store/\n    console/             React/Vite console app\n    docs/                Zensical-generated public docs\n  platform-docs/         Zensical Markdown source for public docs\n  packages/\n    sdk/                 Browser/client SDK and shared helpers\n    cli/                 pags CLI: init, publish, login, mcp, runner\n    browser-runner/      Local Playwright/tmux runtime served by `pags up`\n    compliance/          Policy/check tooling\n  agents/                First-party catalog agents\n  templates/             Agent scaffolds\n  docs/                  Internal architecture and planning docs\n</code></pre>","path":["Architecture"],"tags":[]},{"location":"architecture/#system-context","level":2,"title":"System Context","text":"<pre><code>Browser console / widget\n        |\n        v\nworkers/host -------------- static assets, generated docs, widget\n        |\n        v\nworkers/api --------------- auth, agents, instances, runtime, connectors\n  |      |      |      |\n  |      |      |      +-- D1: users, agents, instances, runtime rows, billing\n  |      |      +--------- R2: files, media, screenshots, agent assets\n  |      +---------------- Vectorize: knowledge embeddings\n  +----------------------- Durable Objects: AgentDO, RelayDO\n           |\n           +-- Workflows: JobApplyWorkflow, CodingSessionWorkflow\n\nMCP clients\n        |\n        v\nworkers/mcp --------------- OAuth + tool surface, calls workers/api\n\nLocal machine\n        |\n        v\npackages/browser-runner ---- Playwright browser, tmux CLIs, local files\n        ^\n        |\nRelayDO WebSocket relay ---- outbound runner connection, no inbound tunnel\n</code></pre>","path":["Architecture"],"tags":[]},{"location":"architecture/#control-plane","level":2,"title":"Control Plane","text":"<p>The control plane is the Cloudflare-hosted platform:</p> <ul> <li><code>workers/api</code>: product API, orchestration, auth, connector OAuth, runtime registration, instance lifecycle, billing, analytics, storage routing.</li> <li><code>workers/mcp</code>: MCP tool interface with OAuth scopes, confirmation gates, dry-run support, and audit logging.</li> <li><code>workers/host</code>: static web host for the store, console, docs, widget, and public assets.</li> </ul> <p>The control plane owns authorization, persistence, durable workflow state, and user-visible operational records.</p>","path":["Architecture"],"tags":[]},{"location":"architecture/#state-plane","level":2,"title":"State Plane","text":"<p>State is split intentionally:</p> <ul> <li>D1 stores account, marketplace, instance, runtime, billing, OAuth-token ciphertext metadata, board, events, errors, and workflow/task mirrors.</li> <li>Durable Object storage stores per-agent runtime state: messages, memory, knowledge docs, tasks, collections, activity, file metadata, summaries.</li> <li>R2 stores binary files and large artifacts.</li> <li>Vectorize stores semantic vectors for knowledge and repo/file retrieval.</li> </ul>","path":["Architecture"],"tags":[]},{"location":"architecture/#runtime-plane","level":2,"title":"Runtime Plane","text":"<p>Runtime-backed agents use <code>pags up</code>, which runs a local browser/CLI runtime from <code>packages/browser-runner</code>. The runner connects outbound to a per-instance <code>RelayDO</code>, so the cloud can call local capabilities without opening inbound ports.</p> <p>Current runtime-backed surfaces:</p> <ul> <li>Job Application Assistant: Cloudflare Workflow brain drives Playwright browser actions.</li> <li>Coder: Cloudflare Workflow brain drives local CLI sessions in tmux.</li> </ul>","path":["Architecture"],"tags":[]},{"location":"architecture/#docs-plane","level":2,"title":"Docs Plane","text":"<p>Zensical is the standard generator for public platform docs.</p> <pre><code>platform-docs/*.md -&gt; python3 -m zensical build --strict -&gt; store/docs/**\n</code></pre> <p>The Host Worker build inlines the generated <code>store/docs</code> tree so <code>/docs/</code>, <code>/docs/search.json</code>, <code>/docs/assets/...</code>, and generated article pages are served from the same Worker as the main site.</p>","path":["Architecture"],"tags":[]},{"location":"browser-runtime/","level":1,"title":"ProAgentStore Browser Runtime","text":"<p>Runtime-backed agents use <code>pags up</code>, which runs local browser and CLI capabilities from <code>packages/browser-runner</code>.</p> <p>The current PAGS runtime pattern is:</p> <pre><code>Workflow brain -&gt; callRunner() -&gt; RelayDO -&gt; WebSocket -&gt; local runner -&gt; browser/tmux\n</code></pre> <p>The cloud-hosted workflow is the brain. The local runner is the hands. This keeps account state, audit, and orchestration in ProAgentStore while browser sessions, local files, and CLI sessions stay on the user's machine.</p>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#connectivity-modes","level":2,"title":"Connectivity Modes","text":"<p><code>pags up</code> connects outbound to the ProAgentStore WebSocket relay. The local machine does not need to expose an inbound port.</p> <pre><code>ProAgentStore Workflow\n  -&gt; RelayDO\n  -&gt; outbound WebSocket\n  -&gt; local browser runner\n  -&gt; Playwright browser or tmux CLI\n</code></pre> <p>Cloudflare Tunnel remains a fallback/debug path for older runtime registrations, but the default model is outbound WebSocket relay.</p>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#brain-vs-hands","level":2,"title":"Brain vs Hands","text":"<p>The workflow brain runs in ProAgentStore. The hands run locally.</p> Piece Where It Runs Responsibility Brain Cloudflare Workflow Plans steps, manages durable task state, checks policy, requests approval Relay RelayDO WebSocket relay Carries scoped calls between the cloud workflow and local runner Hands Local <code>pags up</code> runner Drives Playwright browser actions or tmux CLI sessions Browser/session User machine Keeps cookies, local files, active logins, and interactive handoff state","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#browser-capable-agents","level":2,"title":"Browser-Capable Agents","text":"<p>Browser-capable agents are useful when a task needs:</p> <ul> <li>saved browser logins</li> <li>real page interaction</li> <li>file uploads and downloads</li> <li>manual handoff</li> <li>long-running task state</li> <li>screenshots or browser event traces</li> </ul> <p>The Job Application Assistant is the reference browser-capable agent. Its Cloudflare Workflow brain drives a local Playwright browser through snapshot/action steps and can pause for user approval.</p>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#coder-agents","level":2,"title":"Coder Agents","text":"<p>Coder agents use the same runtime idea, but the local capability is a tmux-backed CLI session rather than a browser.</p> <pre><code>Workflow brain -&gt; RelayDO -&gt; local runner -&gt; tmux CLI session\n</code></pre> <p>This allows long-running coding sessions while preserving the account-level control plane and audit model.</p>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#safety-rules","level":2,"title":"Safety Rules","text":"<ul> <li>The runner must authenticate to ProAgentStore.</li> <li>Runtime tasks are scoped to a private instance.</li> <li>Destructive actions require explicit confirmation where supported.</li> <li>Browser tasks can require approval before final submission.</li> <li>Local files and browser profiles stay local unless the user intentionally uploads or imports content.</li> </ul>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"browser-runtime/#when-not-to-use-a-browser-runtime","level":2,"title":"When Not To Use A Browser Runtime","text":"<p>Most server-only agents should use hosted Worker execution or standard instance chat. Do not require <code>pags up</code> for agents that only need text processing, API calls, knowledge search, or document drafting.</p>","path":["ProAgentStore Browser Runtime"],"tags":[]},{"location":"connectors/","level":1,"title":"Connectors","text":"<p>ProAgentStore connectors are account-level integrations with per-agent grants.</p> <p>The user connects Google Drive, Google Docs, Zoho WorkDrive, Gmail, or another provider to their ProAgentStore account. After connection, the user chooses which agents can access which folders, shared drives, or provider resources.</p>","path":["Connectors"],"tags":[]},{"location":"connectors/#recommended-permission-model","level":2,"title":"Recommended Permission Model","text":"<p>Connect providers to the account, not to individual agents.</p> <pre><code>User account\n  -&gt; provider connection\n  -&gt; folder/shared-drive grants\n  -&gt; agent access\n</code></pre> <p>This avoids repeated OAuth flows, keeps revocation simple, and lets the user reuse one provider connection across many agents without giving every agent blanket access.</p>","path":["Connectors"],"tags":[]},{"location":"connectors/#google-docs-through-google-drive","level":2,"title":"Google Docs Through Google Drive","text":"<p>Google Docs files are Drive files with Google Docs MIME types. To let an agent work with Google Docs, connect Google Drive and grant the agent access to the relevant folders or shared drives.</p> <p>Production OAuth callback:</p> <pre><code>https://api.proagentstore.online/v1/drive/google/callback\n</code></pre> <p>Worker configuration:</p> <pre><code>GOOGLE_CLIENT_ID=&lt;google-oauth-client-id&gt;\nGOOGLE_CLIENT_SECRET=&lt;worker secret&gt;\n</code></pre> <p>Cloudflare secret:</p> <pre><code>wrangler secret put GOOGLE_CLIENT_SECRET\n</code></pre>","path":["Connectors"],"tags":[]},{"location":"connectors/#client-flow","level":2,"title":"Client Flow","text":"<ol> <li>The user opens the ProAgentStore console.</li> <li>The user chooses Connect Google Drive.</li> <li>Google asks the user to approve the OAuth consent screen.</li> <li>ProAgentStore stores the encrypted account-level connection.</li> <li>The user grants one or more folders or shared drives to an agent.</li> <li>The agent imports, searches, reads, or writes only within those grants.</li> </ol>","path":["Connectors"],"tags":[]},{"location":"connectors/#folder-grants","level":2,"title":"Folder Grants","text":"<p>Folder grants are the boundary agents should use by default.</p> <ul> <li>Grant the smallest folder that contains the documents the agent needs.</li> <li>Prefer a shared drive or dedicated project folder for team workflows.</li> <li>Avoid whole-drive access unless the agent is explicitly an account-wide document manager.</li> <li>Revoke a folder grant when an agent no longer needs it.</li> </ul>","path":["Connectors"],"tags":[]},{"location":"connectors/#zoho-workdrive","level":2,"title":"Zoho WorkDrive","text":"<p>Zoho WorkDrive follows the same model:</p> <ol> <li>Connect the Zoho account to the user account.</li> <li>Store the encrypted connection and refresh metadata.</li> <li>Let the user grant selected WorkDrive folders or team folders to agents.</li> <li>Enforce grants server-side before import, read, write, or search actions.</li> </ol> <p>Nested WorkDrive folders must preserve the provider folder identity chain so grants on a parent folder can authorize descendants without confusing sibling folders.</p>","path":["Connectors"],"tags":[]},{"location":"connectors/#agent-access-rules","level":2,"title":"Agent Access Rules","text":"<p>Agents should never receive raw provider credentials.</p> <p>Agents receive scoped platform capabilities:</p> <ul> <li>list granted folders</li> <li>search granted files</li> <li>import files into instance knowledge</li> <li>read allowed files</li> <li>write only when the grant and tool scope allow it</li> </ul> <p>All access should be checked against the authenticated ProAgentStore user, the private agent instance, and the provider grant.</p>","path":["Connectors"],"tags":[]},{"location":"connectors/#troubleshooting","level":2,"title":"Troubleshooting","text":"<p>If Google Drive or Google Docs connection fails:</p> <ul> <li>Confirm the OAuth app callback exactly matches <code>https://api.proagentstore.online/v1/drive/google/callback</code>.</li> <li>Confirm <code>GOOGLE_CLIENT_ID</code> is configured in the API worker environment.</li> <li>Confirm <code>GOOGLE_CLIENT_SECRET</code> is set as a Worker secret.</li> <li>Confirm the user approved the requested scopes.</li> <li>Confirm the agent has a folder or shared-drive grant after the account connection succeeds.</li> </ul> <p>If an agent cannot see a document:</p> <ul> <li>Confirm the document is in a granted folder or shared drive.</li> <li>Confirm the provider account itself can access the document.</li> <li>Confirm the grant belongs to the same ProAgentStore account and private instance.</li> <li>Disconnect and reconnect only when OAuth refresh fails; missing grants should be fixed through folder access.</li> </ul>","path":["Connectors"],"tags":[]},{"location":"mcp/","level":1,"title":"ProAgentStore MCP","text":"<p>ProAgentStore is MCP-first. Agents should operate account state through the official MCP server instead of scraping the UI or calling private APIs directly.</p> <p>Remote MCP endpoint:</p> <pre><code>https://mcp.proagentstore.online/mcp\n</code></pre> <p>Human and AI docs:</p> <pre><code>https://proagentstore.online/docs/mcp/\nhttps://proagentstore.online/llms.txt\nhttps://proagentstore.online/.well-known/mcp-server.json\n</code></pre>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#use-it-now","level":2,"title":"Use It Now","text":"<p>Codex:</p> <pre><code>codex mcp add proagentstore --url https://mcp.proagentstore.online/mcp\ncodex mcp list\n# If the server shows \"Not logged in\":\ncodex mcp login proagentstore\n</code></pre> <p>Claude Code:</p> <pre><code>claude mcp add --transport http proagentstore https://mcp.proagentstore.online/mcp\nclaude mcp list\n</code></pre> <p>Generic MCP client:</p> <pre><code>npx mcp-remote https://mcp.proagentstore.online/mcp\n</code></pre> <p>First-party local <code>npx</code> proxy:</p> <pre><code>npx @proagentstore/cli mcp\n</code></pre>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#project-config","level":2,"title":"Project Config","text":"<p>Use <code>.mcp.json</code> for MCP clients that support project-local config:</p> <pre><code>{\n  \"mcpServers\": {\n    \"proagentstore\": {\n      \"type\": \"streamable-http\",\n      \"url\": \"https://mcp.proagentstore.online/mcp\"\n    }\n  }\n}\n</code></pre> <p>Use <code>.codex/config.toml</code> for Codex project-local config in trusted repos:</p> <pre><code>[mcp_servers.proagentstore]\nurl = \"https://mcp.proagentstore.online/mcp\"\ntool_timeout_sec = 120\ndefault_tools_approval_mode = \"prompt\"\n</code></pre> <p>For stdio-only MCP clients, use the published CLI as the local proxy:</p> <pre><code>{\n  \"mcpServers\": {\n    \"proagentstore\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@proagentstore/cli\", \"mcp\"]\n    }\n  }\n}\n</code></pre>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#agent-rules","level":2,"title":"Agent Rules","text":"<pre><code>Use ProAgentStore only through the configured MCP server.\n\nDo not use the web UI.\nDo not call REST/GraphQL/private APIs directly.\nFirst inspect available MCP tools/resources.\nPrefer read-only tools unless the task explicitly requires changes.\nConfirm before destructive actions.\n</code></pre>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#auth-scopes-and-safety","level":2,"title":"Auth Scopes And Safety","text":"<p>OAuth connections can be scoped to:</p> <ul> <li><code>read</code></li> <li><code>write</code></li> <li><code>runtime</code></li> <li><code>destructive</code></li> </ul> <p>If a client does not request a ProAgentStore-specific scope, or only requests standard OAuth/OIDC scopes such as <code>openid email profile</code>, ProAgentStore grants the existing default scope set for compatibility. Use <code>read</code> only for inspection agents. Set <code>MCP_READ_ONLY=1</code> on the MCP worker to force server-wide read-only mode.</p> <p>Mutating tools support <code>dry_run: true</code> where useful. Destructive or overwrite-style tools require an exact <code>confirm</code> value:</p> <ul> <li><code>write_agent_file</code>: <code>confirm: \"write_agent_file\"</code></li> <li><code>batch_write_agent_files</code>: <code>confirm: \"batch_write_agent_files\"</code></li> <li><code>unregister_instance_runtime</code>: <code>confirm: \"unregister_instance_runtime\"</code></li> <li><code>cancel_instance_task</code>: <code>confirm: \"cancel_instance_task\"</code></li> <li><code>delete_instance_knowledge</code>: <code>confirm: \"delete_instance_knowledge\"</code></li> <li><code>delete_instance_trigger</code>: <code>confirm: \"delete_instance_trigger\"</code></li> <li><code>cancel_instance</code>: <code>confirm: \"cancel_instance\"</code></li> </ul> <p>Use <code>mcp_audit_log</code> to inspect recent MCP write, runtime, dry-run, denied, and destructive tool events for the authenticated account.</p>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#correct-runtime-flows","level":2,"title":"Correct Runtime Flows","text":"<p>Public trial preview:</p> <pre><code>list_agents -&gt; chat_with_agent\n</code></pre> <p>Private instance runtime:</p> <pre><code>list_agents -&gt; subscribe_agent -&gt; my_instances -&gt; add_instance_knowledge -&gt; chat_with_instance -&gt; instance_messages\n</code></pre> <p>Private instance triggers:</p> <pre><code>my_instances -&gt; list_instance_triggers -&gt; create_instance_trigger -&gt; run_instance_trigger -&gt; list_instance_trigger_events\n</code></pre> <p>Use <code>create_instance_trigger</code> with action <code>sync_connector</code> and config <code>provider</code> plus <code>grant_id</code> to schedule Google Drive or Zoho WorkDrive folder syncs for an already-granted folder.</p> <p>Browser-capable instance runtime:</p> <pre><code>subscribe_agent -&gt; register_instance_runtime -&gt; instance_runtime_status -&gt; run_instance_task -&gt; approve_instance_task -&gt; instance_task_events\n</code></pre>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#capabilities","level":2,"title":"Capabilities","text":"<p>Read:</p> <ul> <li>agents</li> <li>private instances</li> <li>messages</li> <li>knowledge</li> <li>deployment status</li> <li>analytics</li> <li>runtime status</li> <li>task events</li> <li>MCP audit log</li> </ul> <p>Write:</p> <ul> <li>create/update agents</li> <li>scaffold repositories</li> <li>write agent files</li> <li>subscribe to agents</li> <li>add instance knowledge</li> <li>register runtimes</li> <li>create, approve, and cancel runtime tasks</li> </ul> <p>Not supported:</p> <ul> <li>user deletion</li> <li>permission changes</li> <li>broad billing changes</li> <li>arbitrary shell execution</li> <li>generic API proxying</li> </ul>","path":["ProAgentStore MCP"],"tags":[]},{"location":"mcp/#security","level":2,"title":"Security","text":"<ul> <li>OAuth/browser sign-in is the default auth path.</li> <li>OAuth scopes are enforced server-side for write, runtime, and destructive tools.</li> <li>Tools are purpose-specific; there is no generic shell or arbitrary API proxy tool.</li> <li>Mutating tools support dry-run previews where useful.</li> <li>Destructive and repository overwrite tools require explicit confirmation.</li> <li>MCP audit events are stored for authenticated OAuth sessions.</li> <li>Browser actions are task-based and can require explicit approval.</li> <li>Private instance runtime uses caller-owned AI credentials.</li> <li>Prefer read-only tools unless the user explicitly requests changes.</li> </ul>","path":["ProAgentStore MCP"],"tags":[]},{"location":"skill-publishing/","level":1,"title":"Skill Publishing","text":"<p>ProAgentStore skills should be published for both Codex and Claude users. The shared unit is the Agent Skills format: a folder with <code>SKILL.md</code>, optional <code>scripts/</code>, <code>references/</code>, and <code>assets/</code>.</p>","path":["Skill Publishing"],"tags":[]},{"location":"skill-publishing/#source-of-truth","level":2,"title":"Source Of Truth","text":"<p>Keep the canonical skill here:</p> <pre><code>skills/proagentstore-mcp-operator/\n</code></pre> <p>That folder follows the open Agent Skills format:</p> <ul> <li><code>SKILL.md</code> has YAML frontmatter with <code>name</code>, <code>description</code>, <code>license</code>, <code>compatibility</code>, and <code>metadata</code>.</li> <li>The body contains the operating workflow.</li> <li>Detailed references or helper scripts should be split into separate files when the skill grows.</li> </ul> <p>The same skill content is copied into platform-specific plugin wrappers:</p> <pre><code>plugins/codex/proagentstore/skills/proagentstore-mcp-operator/\nplugins/claude/proagentstore/skills/proagentstore-mcp-operator/\n</code></pre> <p>Update all copies together until an automated packaging script owns that step.</p>","path":["Skill Publishing"],"tags":[]},{"location":"skill-publishing/#codex-publishing","level":2,"title":"Codex Publishing","text":"<p>Codex treats skills as local authoring units and plugins as the installable distribution unit. Publish ProAgentStore to Codex as a plugin.</p> <p>Repo files:</p> <pre><code>.agents/plugins/marketplace.json\nplugins/codex/proagentstore/.codex-plugin/plugin.json\nplugins/codex/proagentstore/.mcp.json\nplugins/codex/proagentstore/skills/proagentstore-mcp-operator/SKILL.md\n</code></pre> <p>Install from this repo marketplace while developing:</p> <pre><code>codex plugin marketplace add ./platform\n</code></pre> <p>After publishing the repo as the official marketplace source:</p> <pre><code>codex plugin marketplace add ProAgentStore/platform\n</code></pre> <p>Users then install the <code>proagentstore</code> plugin from the Codex plugin browser or marketplace.</p>","path":["Skill Publishing"],"tags":[]},{"location":"skill-publishing/#claude-publishing","level":2,"title":"Claude Publishing","text":"<p>Claude Code supports standalone skills, but shared distribution should use plugins and marketplaces.</p> <p>Repo files:</p> <pre><code>.claude-plugin/marketplace.json\nplugins/claude/proagentstore/.claude-plugin/plugin.json\nplugins/claude/proagentstore/.mcp.json\nplugins/claude/proagentstore/skills/proagentstore-mcp-operator/SKILL.md\n</code></pre> <p>Install from this repo marketplace while developing:</p> <pre><code>/plugin marketplace add ./platform\n/plugin install proagentstore@proagentstore\n/reload-plugins\n</code></pre> <p>After publishing the repo as the official marketplace source:</p> <pre><code>/plugin marketplace add ProAgentStore/platform\n/plugin install proagentstore@proagentstore\n/reload-plugins\n</code></pre>","path":["Skill Publishing"],"tags":[]},{"location":"skill-publishing/#mcp-bundling","level":2,"title":"MCP Bundling","text":"<p>Both plugin wrappers bundle the deployed ProAgentStore MCP endpoint:</p> <pre><code>https://mcp.proagentstore.online/mcp\n</code></pre> <p>Codex wrapper:</p> <pre><code>{\n  \"mcpServers\": {\n    \"proagentstore\": {\n      \"url\": \"https://mcp.proagentstore.online/mcp\"\n    }\n  }\n}\n</code></pre> <p>Claude wrapper:</p> <pre><code>{\n  \"mcpServers\": {\n    \"proagentstore\": {\n      \"type\": \"http\",\n      \"url\": \"https://mcp.proagentstore.online/mcp\"\n    }\n  }\n}\n</code></pre> <p>OAuth remains per user. Installing the plugin does not give ProAgentStore account access until the user approves the MCP sign-in.</p>","path":["Skill Publishing"],"tags":[]},{"location":"skill-publishing/#discovery-checklist","level":2,"title":"Discovery Checklist","text":"<p>Every ProAgentStore skill should have:</p> <ul> <li>A clear <code>description</code> that says when the agent should use it.</li> <li>A public docs page with install commands for Codex and Claude.</li> <li>An entry in <code>/skills/</code>.</li> <li>An entry in <code>/skills.json</code>.</li> <li>Coverage in <code>/llms.txt</code> and <code>/llms-full.txt</code> when it changes the recommended agent workflow.</li> <li>A plugin wrapper for Codex.</li> <li>A plugin wrapper for Claude.</li> <li>A marketplace entry for each ecosystem.</li> <li>MCP dependency metadata when the skill performs account actions.</li> <li>A changelog entry when the skill changes behavior.</li> <li>A smoke test that confirms the MCP server exposes the expected tools.</li> </ul>","path":["Skill Publishing"],"tags":[]},{"location":"triggers/","level":1,"title":"Triggers","text":"<p>Triggers let a private ProAgentStore instance start work from outside the chat UI.</p> <p>Use triggers for:</p> <ul> <li>inbound webhooks from Zapier, Make, n8n, forms, product events, or custom apps</li> <li>recurring cron schedules for digests, syncs, monitoring, and reminders</li> <li>scheduled Google Drive and Zoho WorkDrive folder syncs into knowledge</li> </ul> <p>Triggers are configured per private instance. A creator can build an agent that supports event-driven work, but each client controls their own webhook URL, schedule, connector grants, and trigger actions.</p>","path":["Triggers"],"tags":[]},{"location":"triggers/#trigger-types","level":2,"title":"Trigger Types","text":"<p>Webhook triggers expose a high-entropy URL:</p> <pre><code>POST https://api.proagentstore.online/v1/triggers/webhook/&lt;token&gt;\n</code></pre> <p>Cron triggers are dispatched by ProAgentStore's API worker scheduler. The platform checks due triggers every minute and advances the next run before dispatch so a failing trigger cannot loop continuously.</p>","path":["Triggers"],"tags":[]},{"location":"triggers/#actions","level":2,"title":"Actions","text":"<p>The first trigger actions are intentionally narrow:</p> <ul> <li><code>create_task</code>: create an instance task from the payload.</li> <li><code>add_knowledge</code>: add the payload as an instance knowledge document.</li> <li><code>sync_connector</code>: import new or changed files from a granted Drive or   WorkDrive folder.</li> <li><code>log_event</code>: record the event without changing agent state.</li> </ul> <p>There is no arbitrary shell, generic API proxy, or hidden platform-owned AI spend path. Triggered work lands in the same instance state, board, knowledge, and audit surface as manual work.</p>","path":["Triggers"],"tags":[]},{"location":"triggers/#connector-sync","level":2,"title":"Connector Sync","text":"<p>Folder sync triggers use the same account-level connector and per-agent grant model as manual imports. Connect Google Drive or Zoho WorkDrive once, grant a folder to an agent instance, then create a trigger with action <code>sync_connector</code>.</p> <p>Example config:</p> <pre><code>{\n  \"provider\": \"google_drive\",\n  \"grantId\": \"grant_uuid\",\n  \"limit\": 10\n}\n</code></pre> <p>Each trigger keeps a file fingerprint ledger, so later runs skip unchanged files instead of importing duplicates.</p>","path":["Triggers"],"tags":[]},{"location":"triggers/#schedules","level":2,"title":"Schedules","text":"<p>Supported schedule forms:</p> <pre><code>@hourly\n@daily\n@weekly\nevery 15 minutes\n0 8 * * *\n</code></pre> <p>The MVP supports simple 5-field cron expressions with numeric or <code>*</code> fields. It does not yet support ranges, lists, steps, timezones, or daylight-saving handling.</p>","path":["Triggers"],"tags":[]},{"location":"triggers/#api","level":2,"title":"API","text":"<p>Authenticated management routes:</p> <pre><code>GET    /v1/triggers?instanceId=&lt;instance-id&gt;\nPOST   /v1/triggers\nPUT    /v1/triggers/:id\nDELETE /v1/triggers/:id\nPOST   /v1/triggers/:id/run\nGET    /v1/triggers/:id/events\n</code></pre> <p>Create body:</p> <pre><code>{\n  \"instanceId\": \"instance_uuid\",\n  \"name\": \"Daily digest\",\n  \"type\": \"cron\",\n  \"action\": \"create_task\",\n  \"schedule\": \"@daily\"\n}\n</code></pre> <p>Webhook body example:</p> <pre><code>{\n  \"title\": \"New lead\",\n  \"description\": \"Acme asked for an enterprise quote.\",\n  \"sourceUrl\": \"https://example.com/leads/123\"\n}\n</code></pre>","path":["Triggers"],"tags":[]},{"location":"triggers/#current-limitations","level":2,"title":"Current Limitations","text":"<ul> <li>No payload-mapping UI yet.</li> <li>No retry policy UI yet.</li> <li>No timezone-aware schedule editor yet.</li> </ul>","path":["Triggers"],"tags":[]}]}